

Model is compatible with any kind of transformer


Transformer Class:

init_state() -> tensor[n_layers, batch_size, ...]

state_forward(ids, state) -> next_state

forward(ids, state) -> ids, next_state

